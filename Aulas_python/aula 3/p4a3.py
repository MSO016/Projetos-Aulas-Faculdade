real= int(input("quantos reais?"))
centavos= real*100
print ("em  centavos voce tem:" + str(centavos))

real= float(input("quantos reais?"))
centavos= real*100
print ("em  centavos voce tem:" + str(centavos))
